<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Blog - Koj Agency" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="UTF-8">

    <!-- SEO Meta Tags -->
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : 'Koj Agency Blog - Insights on digital marketing, web development, and technology trends.' %>">
    <meta name="keywords" content="<%= content_for?(:keywords) ? yield(:keywords) : 'digital marketing, web development, technology, blog' %>">
    <% if content_for?(:canonical_url) %>
      <link rel="canonical" href="<%= yield(:canonical_url) %>">
    <% end %>

    <!-- Open Graph Meta Tags -->
    <meta property="og:type" content="<%= content_for?(:og_type) ? yield(:og_type) : 'website' %>">
    <meta property="og:title" content="<%= content_for?(:og_title) ? yield(:og_title) : yield(:title) %>">
    <meta property="og:description" content="<%= content_for?(:og_description) ? yield(:og_description) : yield(:description) %>">
    <% if content_for?(:og_image) %>
      <meta property="og:image" content="<%= yield(:og_image) %>">
    <% end %>
    <meta property="og:url" content="<%= request.original_url %>">

    <!-- Twitter Card Meta Tags -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= content_for?(:twitter_title) ? yield(:twitter_title) : yield(:title) %>">
    <meta name="twitter:description" content="<%= content_for?(:twitter_description) ? yield(:twitter_description) : yield(:description) %>">
    <% if content_for?(:twitter_image) %>
      <meta name="twitter:image" content="<%= yield(:twitter_image) %>">
    <% end %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

    <!-- RSS Feed -->
    <%= auto_discovery_link_tag(:rss, feed_blog_posts_url(format: :rss), title: "Koj Agency Blog RSS Feed") %>
  </head>

  <body>
    <!-- Main Navigation (shared with main site) -->
    <%= render "shared/navbar" %>

    <!-- Blog Navigation -->
    <%= render 'shared/blog_navigation' %>

    <!-- Flash Messages -->
    <% if notice.present? %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3" role="alert">
        <p><%= notice %></p>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3" role="alert">
        <p><%= alert %></p>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="min-h-screen bg-white pt-16 lg:pt-20">
      <%= yield %>
    </main>

    <!-- Footer -->
    <%= render "shared/footer" %>
  </body>
</html>