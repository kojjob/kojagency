<!-- Hero Section with Enhanced Animations -->
<section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-indigo-50 via-white to-purple-50" data-controller="hero framer">
  <!-- Gradient Mesh Background -->
  <div class="absolute inset-0 overflow-hidden">
    <!-- Animated gradient orbs -->
    <div class="absolute -top-40 -right-40 w-96 h-96 bg-gradient-to-r from-purple-400 to-pink-400 rounded-full mix-blend-multiply filter blur-3xl opacity-70 animate-blob"></div>
    <div class="absolute -bottom-40 -left-40 w-96 h-96 bg-gradient-to-r from-indigo-400 to-blue-400 rounded-full mix-blend-multiply filter blur-3xl opacity-70 animate-blob animation-delay-2000"></div>
    <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-96 h-96 bg-gradient-to-r from-cyan-300 to-purple-300 rounded-full mix-blend-multiply filter blur-3xl opacity-60 animate-blob animation-delay-4000"></div>

    <!-- Floating geometric shapes -->
    <div class="absolute top-20 left-10 w-20 h-20 border-2 border-indigo-300 rounded-lg transform rotate-45 animate-float opacity-30"></div>
    <div class="absolute bottom-32 right-20 w-16 h-16 border-2 border-purple-300 rounded-full animate-float animation-delay-2000 opacity-30"></div>
    <div class="absolute top-1/3 right-10 w-12 h-12 border-2 border-pink-300 transform rotate-12 animate-float animation-delay-3000 opacity-30"></div>

    <!-- Grid pattern overlay -->
    <div class="absolute inset-0 bg-grid-pattern opacity-5"></div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-24 text-center">
    <div class="space-y-8" data-animate-immediate>
      <!-- Animated Badge with pulse -->
      <div class="inline-flex items-center space-x-2 bg-white/90 backdrop-blur-sm px-4 py-2 rounded-full shadow-lg animate-slide-down" data-framer-target="fadeIn">
        <span class="flex h-2 w-2 relative">
          <span class="animate-ping absolute inline-flex h-2 w-2 rounded-full bg-green-400 opacity-75"></span>
          <span class="relative inline-flex rounded-full h-2 w-2 bg-green-500"></span>
        </span>
        <span class="text-sm font-medium text-gray-700">🚀 Available for new projects</span>
      </div>

      <!-- Main Headline with gradient animation -->
      <h1 class="text-5xl md:text-6xl lg:text-8xl font-black leading-tight" data-framer-target="slideUp" data-delay="100">
        <span class="inline-block bg-gradient-to-r from-indigo-600 via-purple-600 to-pink-600 bg-clip-text text-transparent animate-gradient-x">
          Transform Ideas
        </span>
        <br>
        <span class="inline-block text-gray-900 mt-2">Into Digital</span>
        <br>
        <span class="inline-block bg-gradient-to-r from-pink-600 via-purple-600 to-indigo-600 bg-clip-text text-transparent animate-gradient-x-reverse">
          Excellence ✨
        </span>
      </h1>

      <!-- Subheadline with typewriter effect -->
      <p class="max-w-3xl mx-auto text-xl md:text-2xl text-gray-600 leading-relaxed" data-framer-target="fadeIn" data-delay="200">
        We craft <span class="font-semibold text-indigo-600">innovative web applications</span>,
        <span class="font-semibold text-purple-600">mobile experiences</span>, and
        <span class="font-semibold text-pink-600">data solutions</span> that drive growth and deliver
        measurable results for forward-thinking businesses.
      </p>

      <!-- Enhanced CTA Buttons using Design System -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center pt-4" data-framer-target="scale" data-delay="300">
        <%= render "shared/components/button",
            text: "Start Your Project",
            url: contact_path,
            variant: :primary,
            size: :xl,
            icon: true %>

        <%= render "shared/components/button",
            text: "View Our Work",
            url: projects_path,
            variant: :secondary,
            size: :xl,
            icon: true %>
      </div>

      <!-- Trust Indicators with brand logos -->
      <div class="pt-12" data-framer-target="stagger" data-delay="400">
        <p class="text-sm font-semibold text-gray-500 uppercase tracking-wider mb-6 flex items-center justify-center gap-2">
          <svg class="w-5 h-5 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
            <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path>
          </svg>
          Trusted by 150+ innovative companies
        </p>
        <div class="flex flex-wrap justify-center items-center gap-12">
          <% 6.times do |i| %>
            <div class="group relative w-36 h-16 bg-white/80 backdrop-blur-sm rounded-xl flex items-center justify-center shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-1 opacity-0" style="animation-delay: <%= i * 100 %>ms">
              <div class="absolute inset-0 bg-gradient-to-r from-indigo-500 to-purple-500 rounded-xl opacity-0 group-hover:opacity-10 transition-opacity"></div>
              <svg class="w-20 h-8 text-gray-400 group-hover:text-indigo-600 transition-colors" fill="currentColor" viewBox="0 0 120 40">
                <text x="10" y="25" font-family="Arial, sans-serif" font-size="20" font-weight="bold">LOGO</text>
              </svg>
            </div>
          <% end %>
        </div>
      </div>
    </div>

    <!-- Scroll Indicator with animated arrow -->
    <div class="absolute bottom-12 left-1/2 transform -translate-x-1/2" data-framer-target="fadeIn" data-delay="600">
      <div class="flex flex-col items-center gap-2 cursor-pointer group" onclick="document.querySelector('#stats-section').scrollIntoView({ behavior: 'smooth' })">
        <span class="text-sm text-gray-500 font-medium">Scroll to explore</span>
        <div class="w-10 h-16 border-2 border-gray-300 rounded-full flex items-start justify-center p-2 group-hover:border-indigo-500 transition-colors">
          <div class="w-1.5 h-3 bg-gray-400 rounded-full animate-scroll-down group-hover:bg-indigo-500"></div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Stats Section - Enhanced -->
<section id="stats-section" class="py-24 bg-white relative overflow-hidden">
  <!-- Background Effects -->
  <div class="absolute inset-0">
    <div class="absolute inset-0 bg-gradient-to-r from-indigo-50 via-purple-50 to-pink-50 opacity-50"></div>
    <div class="absolute top-0 left-0 w-64 h-64 bg-gradient-to-br from-indigo-200 to-purple-200 rounded-full filter blur-3xl opacity-20 animate-blob"></div>
    <div class="absolute bottom-0 right-0 w-64 h-64 bg-gradient-to-br from-purple-200 to-pink-200 rounded-full filter blur-3xl opacity-20 animate-blob animation-delay-2000"></div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header using Design System -->
    <%= render "shared/components/section_header",
        title: "Our <span>Impact</span> in Numbers",
        subtitle: "Delivering measurable results that drive business growth",
        gradient: :primary %>

    <!-- Stats Grid -->
    <div class="grid grid-cols-2 lg:grid-cols-4 gap-8">
      <% @stats.each_with_index do |stat, index| %>
        <%= render "shared/components/card", variant: :gradient_border, hover: true do %>
          <!-- Icon Container using Design System -->
          <%= render "shared/components/icon_container", gradient: :primary, size: :lg do %>
            <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <% case index %>
              <% when 0 %>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              <% when 1 %>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
              <% when 2 %>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
              <% else %>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 10h4.764a2 2 0 011.789 2.894l-3.5 7A2 2 0 0115.263 21h-4.017c-.163 0-.326-.02-.485-.06L7 20m7-10V5a2 2 0 00-2-2h-.095c-.5 0-.905.405-.905.905 0 .714-.211 1.412-.608 2.006L7 11v9m7-10h-2M7 20H5a2 2 0 01-2-2v-6a2 2 0 012-2h2.5"></path>
              <% end %>
            </svg>
          <% end %>

          <!-- Static Value Display (will add counter animation later) -->
          <div class="text-5xl lg:text-6xl font-black text-transparent bg-clip-text bg-gradient-to-r from-indigo-600 via-purple-600 to-pink-600 mb-3">
            <%= stat[:value] %>
          </div>

          <!-- Label -->
          <div class="text-gray-700 font-semibold text-lg"><%= stat[:label] %></div>

          <!-- Decorative Element -->
          <div class="absolute top-4 right-4 w-20 h-20 bg-gradient-to-br from-indigo-100 to-purple-100 rounded-full opacity-0 group-hover:opacity-100 transition-opacity duration-500 -z-10"></div>
        <% end %>
      <% end %>
    </div>
  </div>
</section>

<!-- Services Section - Enhanced -->
<section class="py-24 bg-gradient-to-b from-gray-50 via-white to-indigo-50 relative overflow-hidden">
  <!-- Background Decoration -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute top-0 right-0 w-96 h-96 bg-gradient-to-br from-indigo-100 to-purple-100 rounded-full filter blur-3xl opacity-30 animate-float"></div>
    <div class="absolute bottom-0 left-0 w-96 h-96 bg-gradient-to-br from-purple-100 to-pink-100 rounded-full filter blur-3xl opacity-30 animate-float animation-delay-2000"></div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header using Design System -->
    <%= render "shared/components/section_header",
        title: "Services That <span>Drive Results</span>",
        subtitle: "From concept to deployment, we deliver end-to-end solutions tailored to your unique business needs with cutting-edge technology and proven methodologies.",
        badge: "🚀 What We Do",
        gradient: :primary %>

    <!-- Services Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      <% @services.each_with_index do |service, index| %>
        <%= render "shared/components/card", variant: :gradient_border, hover: true, padding: :lg do %>
          <!-- Animated Icon Container using Design System -->
          <div class="relative mb-8">
            <%= render "shared/components/icon_container", gradient: :primary, size: :lg do %>
              <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <% if service[:icon] == "web" %>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9"></path>
                <% elsif service[:icon] == "mobile" %>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
                <% elsif service[:icon] == "data" %>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 7v10c0 2.21 3.582 4 8 4s8-1.79 8-4V7M4 7c0 2.21 3.582 4 8 4s8-1.79 8-4M4 7c0-2.21 3.582-4 8-4s8 1.79 8 4m0 5c0 2.21-3.582 4-8 4s-8-1.79-8-4"></path>
                <% else %>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
                <% end %>
              </svg>
            <% end %>
            <!-- Decorative circle -->
            <div class="absolute -top-2 -right-2 w-6 h-6 bg-gradient-to-br from-pink-400 to-purple-400 rounded-full opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
          </div>

          <!-- Title -->
          <h3 class="text-2xl font-bold text-gray-900 mb-4 group-hover:text-transparent group-hover:bg-clip-text group-hover:bg-gradient-to-r group-hover:from-indigo-600 group-hover:to-purple-600 transition-all duration-300">
            <%= service[:title] %>
          </h3>

          <!-- Description -->
          <p class="text-gray-600 mb-6 leading-relaxed">
            <%= service[:description] %>
          </p>

          <!-- Features with animated icons -->
          <ul class="space-y-3 mb-8">
            <% service[:features].each_with_index do |feature, f_index| %>
              <li class="flex items-start text-sm text-gray-700 group/item" style="animation-delay: <%= (index * 100) + (f_index * 50) %>ms">
                <svg class="w-5 h-5 text-indigo-500 mr-3 mt-0.5 flex-shrink-0 group-hover/item:scale-110 transition-transform" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                </svg>
                <span class="flex-1"><%= feature %></span>
              </li>
            <% end %>
          </ul>

          <!-- CTA Link -->
          <%= link_to services_path, class: "inline-flex items-center justify-between w-full px-6 py-3 bg-gradient-to-r from-indigo-50 to-purple-50 text-indigo-700 font-bold rounded-xl group-hover:from-indigo-600 group-hover:to-purple-600 group-hover:text-white transition-all duration-300 shadow-sm group-hover:shadow-lg" do %>
            <span>Learn More</span>
            <svg class="w-5 h-5 transform group-hover:translate-x-2 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
            </svg>
          <% end %>

          <!-- Background Pattern -->
          <div class="absolute bottom-0 right-0 w-32 h-32 bg-gradient-to-br from-indigo-50 to-purple-50 rounded-full -mr-16 -mb-16 opacity-50 group-hover:opacity-100 transition-opacity duration-500"></div>
        <% end %>
      <% end %>
    </div>

    <!-- View All Services CTA -->
    <div class="text-center mt-16" data-framer-target="fadeIn" data-delay="400">
      <%= render "shared/components/button",
          text: "Explore All Services",
          url: services_path,
          variant: :primary,
          size: :xl,
          icon: true %>
    </div>
  </div>
</section>

<!-- Portfolio Section - Enhanced -->
<section class="py-24 bg-white relative overflow-hidden">
  <!-- Background Decoration -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute top-20 left-10 w-72 h-72 bg-gradient-to-br from-indigo-100 to-purple-100 rounded-full filter blur-3xl opacity-20 animate-blob"></div>
    <div class="absolute bottom-20 right-10 w-72 h-72 bg-gradient-to-br from-purple-100 to-pink-100 rounded-full filter blur-3xl opacity-20 animate-blob animation-delay-2000"></div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-20">
      <span class="inline-block px-4 py-2 bg-gradient-to-r from-indigo-100 to-purple-100 text-indigo-700 rounded-full text-sm font-semibold mb-4">
        💼 Our Work
      </span>
      <h2 class="text-4xl lg:text-6xl font-black text-gray-900 mb-6">
        Featured <span class="bg-gradient-to-r from-indigo-600 via-purple-600 to-pink-600 bg-clip-text text-transparent">Case Studies</span>
      </h2>
      <p class="max-w-3xl mx-auto text-xl text-gray-600 leading-relaxed">
        Explore how we've helped businesses achieve extraordinary results through innovative digital solutions and cutting-edge technology.
      </p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
      <% @portfolio_items.each_with_index do |item, index| %>
        <div class="group relative bg-white rounded-3xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-500 transform hover:-translate-y-2">
          <!-- Gradient Border Effect -->
          <div class="absolute -inset-1 bg-gradient-to-br from-indigo-500 via-purple-500 to-pink-500 rounded-3xl opacity-0 group-hover:opacity-100 blur-sm transition-opacity duration-500"></div>

          <div class="relative bg-white rounded-3xl overflow-hidden">
            <!-- Image Placeholder with Enhanced Design -->
            <div class="aspect-w-16 aspect-h-9 h-64 bg-gradient-to-br from-indigo-100 via-purple-100 to-pink-100 relative overflow-hidden">
              <!-- Animated Gradient Overlay -->
              <div class="absolute inset-0 bg-gradient-to-br from-indigo-600/30 to-purple-600/30 group-hover:opacity-0 transition-opacity duration-500"></div>

              <!-- Category Badge -->
              <div class="absolute top-4 left-4">
                <span class="inline-flex items-center px-4 py-2 rounded-full text-sm font-semibold bg-white/95 backdrop-blur-sm text-indigo-600 shadow-lg">
                  <%= item[:category].capitalize %>
                </span>
              </div>

              <!-- Decorative Icon -->
              <div class="absolute inset-0 flex items-center justify-center opacity-20 group-hover:opacity-30 transition-opacity duration-500">
                <svg class="w-32 h-32 text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <% case item[:category] %>
                  <% when 'web' %>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9"></path>
                  <% when 'analytics' %>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
                  <% when 'mobile' %>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
                  <% else %>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 7v10c0 2.21 3.582 4 8 4s8-1.79 8-4V7M4 7c0 2.21 3.582 4 8 4s8-1.79 8-4M4 7c0-2.21 3.582-4 8-4s8 1.79 8 4"></path>
                  <% end %>
                </svg>
              </div>
            </div>

            <!-- Content -->
            <div class="p-8">
              <div class="flex items-center justify-between mb-4">
                <h3 class="text-2xl font-bold text-gray-900 group-hover:text-indigo-600 transition-colors"><%= item[:title] %></h3>
                <span class="px-3 py-1 bg-gradient-to-r from-indigo-50 to-purple-50 text-indigo-700 text-sm font-semibold rounded-full"><%= item[:client] %></span>
              </div>

              <p class="text-gray-600 mb-6 leading-relaxed"><%= item[:description] %></p>

              <!-- Metrics with Enhanced Design -->
              <div class="grid grid-cols-3 gap-4 mb-6 p-4 bg-gradient-to-br from-gray-50 to-indigo-50 rounded-2xl">
                <% item[:metrics].each do |key, value| %>
                  <div class="text-center">
                    <div class="text-2xl lg:text-3xl font-black bg-gradient-to-r from-indigo-600 to-purple-600 bg-clip-text text-transparent"><%= value %></div>
                    <div class="text-xs text-gray-600 uppercase tracking-wide font-semibold mt-1"><%= key.to_s.gsub('_', ' ').capitalize %></div>
                  </div>
                <% end %>
              </div>

              <%= render "shared/components/button",
                  text: "View Case Study",
                  url: projects_path,
                  variant: :primary,
                  size: :base,
                  icon: true %>
            </div>
          </div>
        </div>
      <% end %>
    </div>

    <div class="text-center mt-12">
      <%= render "shared/components/button",
          text: "View All Projects",
          url: projects_path,
          variant: :primary,
          size: :lg,
          icon: true %>
    </div>
  </div>
</section>

<!-- Process Section - Enhanced -->
<section class="py-24 bg-gradient-to-br from-indigo-50 via-purple-50 to-pink-50 relative overflow-hidden">
  <!-- Background Effects -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute top-1/4 left-0 w-96 h-96 bg-gradient-to-br from-indigo-200 to-purple-200 rounded-full filter blur-3xl opacity-20 animate-blob"></div>
    <div class="absolute bottom-1/4 right-0 w-96 h-96 bg-gradient-to-br from-purple-200 to-pink-200 rounded-full filter blur-3xl opacity-20 animate-blob animation-delay-3000"></div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-20">
      <span class="inline-block px-4 py-2 bg-white/80 backdrop-blur-sm text-indigo-700 rounded-full text-sm font-semibold mb-4">
        🎯 How We Work
      </span>
      <h2 class="text-4xl lg:text-6xl font-black text-gray-900 mb-6">
        Our <span class="bg-gradient-to-r from-indigo-600 via-purple-600 to-pink-600 bg-clip-text text-transparent">Process</span>
      </h2>
      <p class="max-w-3xl mx-auto text-xl text-gray-600 leading-relaxed">
        A proven, agile methodology that ensures project success from inception to launch and beyond.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      <% @process_steps.each_with_index do |step, index| %>
        <div class="relative group">
          <!-- Animated Connector Line -->
          <% if index < @process_steps.length - 1 %>
            <div class="hidden lg:block absolute top-20 left-full w-full h-1 -z-10">
              <div class="w-full h-full bg-gradient-to-r from-indigo-400 via-purple-400 to-pink-400 rounded-full animate-pulse"></div>
            </div>
          <% end %>

          <%= render "shared/components/card", variant: :gradient_border, hover: true do %>
            <%= render "shared/components/icon_container", gradient: :primary, size: :lg do %>
              <div class="text-4xl font-black text-white">
                <%= step[:number] %>
              </div>
            <% end %>

            <h3 class="text-2xl font-black text-gray-900 mb-4 group-hover:text-indigo-600 transition-colors"><%= step[:title] %></h3>
            <p class="text-gray-600 leading-relaxed"><%= step[:description] %></p>

            <!-- Decorative Arrow -->
            <% if index < @process_steps.length - 1 %>
              <div class="hidden lg:block absolute -right-4 top-1/2 transform -translate-y-1/2">
                <svg class="w-8 h-8 text-indigo-400 group-hover:text-purple-600 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
                </svg>
              </div>
            <% end %>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</section>

<!-- Testimonials Section - Enhanced -->
<section class="py-24 bg-white overflow-hidden relative">
  <!-- Background Decoration -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute top-0 left-1/4 w-96 h-96 bg-gradient-to-br from-indigo-100 to-purple-100 rounded-full filter blur-3xl opacity-20 animate-float"></div>
    <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-gradient-to-br from-purple-100 to-pink-100 rounded-full filter blur-3xl opacity-20 animate-float animation-delay-2000"></div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-20">
      <span class="inline-block px-4 py-2 bg-gradient-to-r from-indigo-100 to-purple-100 text-indigo-700 rounded-full text-sm font-semibold mb-4">
        💬 Testimonials
      </span>
      <h2 class="text-4xl lg:text-6xl font-black text-gray-900 mb-6">
        What Our <span class="bg-gradient-to-r from-indigo-600 via-purple-600 to-pink-600 bg-clip-text text-transparent">Clients Say</span>
      </h2>
      <p class="max-w-3xl mx-auto text-xl text-gray-600 leading-relaxed">
        Don't just take our word for it. Hear from the businesses we've helped transform with our innovative solutions.
      </p>
    </div>

    <div class="relative" data-controller="carousel">
      <div class="overflow-hidden">
        <div class="flex transition-transform duration-500 ease-in-out" data-carousel-target="container">
          <% @testimonials.each_with_index do |testimonial, index| %>
            <div class="w-full flex-shrink-0 px-4" data-carousel-target="slide">
              <div class="max-w-4xl mx-auto">
                <!-- Enhanced Testimonial Card with Schema.org markup -->
                <div itemscope itemtype="https://schema.org/Review">
                  <%= render "shared/components/card", variant: :gradient_border, hover: true, padding: :lg do %>
                    <!-- Quote Mark -->
                    <div class="absolute top-8 left-8 text-8xl text-indigo-100 font-serif leading-none">"</div>

                    <!-- Rating with schema markup -->
                    <div itemprop="reviewRating" itemscope itemtype="https://schema.org/Rating" class="flex justify-center mb-8 relative z-10">
                      <meta itemprop="ratingValue" content="<%= testimonial.rating %>">
                      <meta itemprop="bestRating" content="5">
                      <meta itemprop="worstRating" content="1">
                      <% testimonial.rating.times do %>
                        <svg class="w-7 h-7 text-yellow-400 drop-shadow-md" fill="currentColor" viewBox="0 0 20 20">
                          <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path>
                        </svg>
                      <% end %>
                    </div>

                    <!-- Quote Content with schema markup -->
                    <blockquote class="text-center relative z-10">
                      <p itemprop="reviewBody" class="text-2xl lg:text-3xl text-gray-800 font-medium leading-relaxed mb-10">
                        <%= testimonial.content %>
                      </p>
                    </blockquote>

                    <!-- Author with Avatar and schema markup -->
                    <div itemprop="author" itemscope itemtype="https://schema.org/Person" class="flex flex-col items-center justify-center relative z-10">
                      <!-- Avatar Circle or Photo -->
                      <% if testimonial.has_photo? %>
                        <img itemprop="image" src="<%= testimonial.client_photo_url %>" alt="<%= testimonial.client_name %>" class="w-20 h-20 rounded-full object-cover mb-4 shadow-lg">
                      <% else %>
                        <%= render "shared/components/icon_container", gradient: :primary, size: :lg do %>
                          <span class="text-2xl font-bold text-white"><%= testimonial.client_name.split.map(&:first).join %></span>
                        <% end %>
                      <% end %>

                      <div class="text-center">
                        <div itemprop="name" class="text-xl font-bold text-gray-900"><%= testimonial.client_name %></div>
                        <div class="text-gray-600 font-medium">
                          <% if testimonial.client_position.present? %>
                            <span itemprop="jobTitle"><%= testimonial.client_position %></span>
                          <% end %>
                          <% if testimonial.client_company.present? %>
                            <span itemprop="worksFor" itemscope itemtype="https://schema.org/Organization">
                              <% if testimonial.client_position.present? %>, <% end %>
                              <span itemprop="name"><%= testimonial.client_company %></span>
                            </span>
                          <% end %>
                        </div>
                      </div>
                    </div>

                    <!-- Hidden schema.org metadata -->
                    <% if testimonial.testimonial_date.present? %>
                      <meta itemprop="datePublished" content="<%= testimonial.testimonial_date.iso8601 %>">
                    <% else %>
                      <meta itemprop="datePublished" content="<%= testimonial.created_at.iso8601 %>">
                    <% end %>

                    <!-- Item reviewed - agency service -->
                    <div itemprop="itemReviewed" itemscope itemtype="https://schema.org/ProfessionalService" style="display:none;">
                      <meta itemprop="name" content="KojAgency Digital Services">
                      <meta itemprop="description" content="Professional digital agency providing web development, mobile apps, data engineering, and analytics platforms">
                    </div>
                  <% end %>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      </div>

      <!-- Navigation -->
      <button class="absolute left-4 top-1/2 transform -translate-y-1/2 bg-white rounded-full p-3 shadow-lg hover:shadow-xl transition-all duration-200" data-action="click->carousel#prev">
        <svg class="w-6 h-6 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
        </svg>
      </button>
      <button class="absolute right-4 top-1/2 transform -translate-y-1/2 bg-white rounded-full p-3 shadow-lg hover:shadow-xl transition-all duration-200" data-action="click->carousel#next">
        <svg class="w-6 h-6 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
        </svg>
      </button>

      <!-- Dots -->
      <div class="flex justify-center mt-8 space-x-2">
        <% @testimonials.each_with_index do |_, index| %>
          <button
            class="w-2 h-2 rounded-full transition-all duration-200 <%= index == 0 ? 'w-8 bg-indigo-600' : 'bg-gray-300 hover:bg-gray-400' %>"
            data-action="click->carousel#goToSlide"
            data-carousel-slide-param="<%= index %>"
          ></button>
        <% end %>
      </div>
    </div>
  </div>
</section>

<!-- CTA Section -->
<section class="py-32 bg-gradient-to-br from-indigo-600 via-purple-600 to-pink-600 relative overflow-hidden">
  <!-- Animated Background Blobs -->
  <div class="absolute inset-0 overflow-hidden">
    <div class="absolute -top-40 -left-40 w-96 h-96 bg-white rounded-full opacity-10 blur-3xl animate-blob"></div>
    <div class="absolute -bottom-40 -right-40 w-96 h-96 bg-white rounded-full opacity-10 blur-3xl animate-blob animation-delay-2000"></div>
    <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-96 h-96 bg-pink-400 rounded-full opacity-10 blur-3xl animate-blob animation-delay-4000"></div>
  </div>

  <!-- Grid Pattern Overlay -->
  <div class="absolute inset-0 opacity-10">
    <div class="w-full h-full" style="background-image: linear-gradient(rgba(255,255,255,0.1) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.1) 1px, transparent 1px); background-size: 40px 40px;"></div>
  </div>

  <div class="relative z-10 max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <!-- Badge Label -->
    <span class="inline-block px-6 py-3 bg-white/20 backdrop-blur-sm text-white rounded-full text-sm font-semibold mb-8 shadow-lg">
      🚀 Let's Build Something Amazing
    </span>

    <!-- Heading -->
    <h2 class="text-5xl lg:text-7xl font-black text-white mb-8 leading-tight">
      Ready to Transform<br/>
      <span class="bg-gradient-to-r from-yellow-200 via-pink-200 to-purple-200 bg-clip-text text-transparent">Your Business?</span>
    </h2>

    <!-- Description -->
    <p class="text-xl lg:text-2xl text-white/90 mb-12 max-w-3xl mx-auto leading-relaxed">
      Let's discuss how we can help you achieve your digital goals and drive meaningful results. Our team is ready to turn your vision into reality.
    </p>

    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
      <%= render "shared/components/button",
          text: "Start Your Project",
          url: contact_path,
          variant: :secondary,
          size: :xl,
          icon: true %>

      <%= render "shared/components/button",
          text: "Schedule a Call",
          url: "tel:+1234567890",
          variant: :outline,
          size: :xl %>
    </div>

    <!-- Trust Signals -->
    <div class="mt-16 flex flex-col sm:flex-row items-center justify-center gap-8 text-white/80">
      <div class="flex items-center gap-2">
        <svg class="w-5 h-5 text-yellow-300" fill="currentColor" viewBox="0 0 20 20">
          <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path>
        </svg>
        <span class="font-semibold">98% Client Satisfaction</span>
      </div>

      <div class="flex items-center gap-2">
        <svg class="w-5 h-5 text-green-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        <span class="font-semibold">150+ Projects Delivered</span>
      </div>

      <div class="flex items-center gap-2">
        <svg class="w-5 h-5 text-blue-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
        </svg>
        <span class="font-semibold">24-48hr Response Time</span>
      </div>
    </div>
  </div>
</section>

<!-- Custom Styles -->
<style>
  @keyframes blob {
    0% { transform: translate(0px, 0px) scale(1); }
    33% { transform: translate(30px, -50px) scale(1.1); }
    66% { transform: translate(-20px, 20px) scale(0.9); }
    100% { transform: translate(0px, 0px) scale(1); }
  }

  .animate-blob {
    animation: blob 7s infinite;
  }

  .animation-delay-2000 {
    animation-delay: 2s;
  }

  .animation-delay-4000 {
    animation-delay: 4s;
  }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fadeIn {
    animation: fadeIn 0.6s ease-out;
  }

  .animate-fadeInUp {
    animation: fadeInUp 0.6s ease-out;
  }
</style>