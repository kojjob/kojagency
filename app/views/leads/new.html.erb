<!-- This view handles the new lead form with error handling -->
<!-- It uses the same form as the contact page but with error display -->

<%= render partial: 'shared/contact_form', locals: { lead: @lead } %>